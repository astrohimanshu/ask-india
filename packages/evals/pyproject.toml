[project]
name = "askindia-evals"
version = "0.1.0"
description = "Evaluation harnesses: execution accuracy, verdict accuracy, judges."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/askindia_evals"]
