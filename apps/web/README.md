# Ask India — web

Next.js front end for the Ask India API. Streams the agent's progress, renders the answer with a
chart driven by the API's chart spec, and shows the executed SQL, dataset and vintage on request.

```bash
pnpm install
NEXT_PUBLIC_API_URL=http://localhost:8000 pnpm dev
```
